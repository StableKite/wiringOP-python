# `wiringPi/mcp23x08.h`

Python-модуль: `wiringop.wiring_pi.mcp23x08`.

Модуль зеркально покрывает публичный API заголовка wiringOP. Имена Python приведены к Python-стилю; рядом сохраняются C-имена для однозначного сопоставления.

## Макросы

- `IODIRA` → `IODIRA`: экспортируется; `value`; определение `0x00`.
- `IPOLA` → `IPOLA`: экспортируется; `value`; определение `0x02`.
- `GPINTENA` → `GPINTENA`: экспортируется; `value`; определение `0x04`.
- `DEFVALA` → `DEFVALA`: экспортируется; `value`; определение `0x06`.
- `INTCONA` → `INTCONA`: экспортируется; `value`; определение `0x08`.
- `IOCON` → `IOCON`: экспортируется; `value`; определение `0x0A`.
- `GPPUA` → `GPPUA`: экспортируется; `value`; определение `0x0C`.
- `INTFA` → `INTFA`: экспортируется; `value`; определение `0x0E`.
- `INTCAPA` → `INTCAPA`: экспортируется; `value`; определение `0x10`.
- `GPIOA` → `GPIO_A`: экспортируется; `value`; определение `0x12`.
- `OLATA` → `OLATA`: экспортируется; `value`; определение `0x14`.
- `IODIRB` → `IODIRB`: экспортируется; `value`; определение `0x01`.
- `IPOLB` → `IPOLB`: экспортируется; `value`; определение `0x03`.
- `GPINTENB` → `GPINTENB`: экспортируется; `value`; определение `0x05`.
- `DEFVALB` → `DEFVALB`: экспортируется; `value`; определение `0x07`.
- `INTCONB` → `INTCONB`: экспортируется; `value`; определение `0x09`.
- `IOCONB` → `IOCONB`: экспортируется; `value`; определение `0x0B`.
- `GPPUB` → `GPPUB`: экспортируется; `value`; определение `0x0D`.
- `INTFB` → `INTFB`: экспортируется; `value`; определение `0x0F`.
- `INTCAPB` → `INTCAPB`: экспортируется; `value`; определение `0x11`.
- `GPIOB` → `GPIO_B`: экспортируется; `value`; определение `0x13`.
- `OLATB` → `OLATB`: экспортируется; `value`; определение `0x15`.
- `IOCON_UNUSED` → `IOCON_UNUSED`: экспортируется; `value`; определение `0x01`.
- `IOCON_INTPOL` → `IOCON_INTPOL`: экспортируется; `value`; определение `0x02`.
- `IOCON_ODR` → `IOCON_ODR`: экспортируется; `value`; определение `0x04`.
- `IOCON_HAEN` → `IOCON_HAEN`: экспортируется; `value`; определение `0x08`.
- `IOCON_DISSLW` → `IOCON_DISSLW`: экспортируется; `value`; определение `0x10`.
- `IOCON_SEQOP` → `IOCON_SEQOP`: экспортируется; `value`; определение `0x20`.
- `IOCON_MIRROR` → `IOCON_MIRROR`: экспортируется; `value`; определение `0x40`.
- `IOCON_BANK_MODE` → `IOCON_BANK_MODE`: экспортируется; `value`; определение `0x80`.
- `IOCON_INIT` → `IOCON_INIT`: экспортируется; `value`; определение `(IOCON_SEQOP)`.
- `CMD_WRITE` → `CMD_WRITE`: экспортируется; `value`; определение `0x40`.
- `CMD_READ` → `CMD_READ`: экспортируется; `value`; определение `0x41`.
