# `wiringPi/ads1115.h`

Python-модуль: `wiringop.wiring_pi.ads1115`.

Модуль зеркально покрывает публичный API заголовка wiringOP. Имена Python приведены к Python-стилю; рядом сохраняются C-имена для однозначного сопоставления.

## Функции

- `ads1115Setup` → `ads1115_setup(pin_base, i2c_address)`; C return `int`; адаптация `direct`.

## Макросы

- `ADS1115_GAIN_6` → `ADS1115_GAIN_6`: экспортируется; `value`; определение `0`.
- `ADS1115_GAIN_4` → `ADS1115_GAIN_4`: экспортируется; `value`; определение `1`.
- `ADS1115_GAIN_2` → `ADS1115_GAIN_2`: экспортируется; `value`; определение `2`.
- `ADS1115_GAIN_1` → `ADS1115_GAIN_1`: экспортируется; `value`; определение `3`.
- `ADS1115_GAIN_HALF` → `ADS1115_GAIN_HALF`: экспортируется; `value`; определение `4`.
- `ADS1115_GAIN_QUARTER` → `ADS1115_GAIN_QUARTER`: экспортируется; `value`; определение `5`.
- `ADS1115_DR_8` → `ADS1115_DR_8`: экспортируется; `value`; определение `0`.
- `ADS1115_DR_16` → `ADS1115_DR_16`: экспортируется; `value`; определение `1`.
- `ADS1115_DR_32` → `ADS1115_DR_32`: экспортируется; `value`; определение `2`.
- `ADS1115_DR_64` → `ADS1115_DR_64`: экспортируется; `value`; определение `3`.
- `ADS1115_DR_128` → `ADS1115_DR_128`: экспортируется; `value`; определение `4`.
- `ADS1115_DR_250` → `ADS1115_DR_250`: экспортируется; `value`; определение `5`.
- `ADS1115_DR_475` → `ADS1115_DR_475`: экспортируется; `value`; определение `6`.
- `ADS1115_DR_860` → `ADS1115_DR_860`: экспортируется; `value`; определение `7`.
