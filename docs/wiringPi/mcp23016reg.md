# `wiringPi/mcp23016reg.h`

Python-модуль: `wiringop.wiring_pi.mcp23016reg`.

Модуль зеркально покрывает публичный API заголовка wiringOP. Имена Python приведены к Python-стилю; рядом сохраняются C-имена для однозначного сопоставления.

## Макросы

- `MCP23016_GP0` → `MCP23016_GP0`: экспортируется; `value`; определение `0x00`.
- `MCP23016_GP1` → `MCP23016_GP1`: экспортируется; `value`; определение `0x01`.
- `MCP23016_OLAT0` → `MCP23016_OLAT0`: экспортируется; `value`; определение `0x02`.
- `MCP23016_OLAT1` → `MCP23016_OLAT1`: экспортируется; `value`; определение `0x03`.
- `MCP23016_IPOL0` → `MCP23016_IPOL0`: экспортируется; `value`; определение `0x04`.
- `MCP23016_IPOL1` → `MCP23016_IPOL1`: экспортируется; `value`; определение `0x05`.
- `MCP23016_IODIR0` → `MCP23016_IODIR0`: экспортируется; `value`; определение `0x06`.
- `MCP23016_IODIR1` → `MCP23016_IODIR1`: экспортируется; `value`; определение `0x07`.
- `MCP23016_INTCAP0` → `MCP23016_INTCAP0`: экспортируется; `value`; определение `0x08`.
- `MCP23016_INTCAP1` → `MCP23016_INTCAP1`: экспортируется; `value`; определение `0x09`.
- `MCP23016_IOCON0` → `MCP23016_IOCON0`: экспортируется; `value`; определение `0x0A`.
- `MCP23016_IOCON1` → `MCP23016_IOCON1`: экспортируется; `value`; определение `0x0B`.
- `IOCON_IARES` → `IOCON_IARES`: экспортируется; `value`; определение `0x01`.
- `IOCON_INIT` → `IOCON_INIT`: экспортируется; `value`; определение `0`.
